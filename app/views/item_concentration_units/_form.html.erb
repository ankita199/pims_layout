<div class="modal fade custom-width" id="item_concentration_unit_modal_<%= item_concentration_unit.id %>" >
	<div class="modal-dialog">
		<div class="modal-content">		
			<div class="modal-header">
				<button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
				<h4 class="modal-title">Item Concentration Unit</h4>
			</div>
			<div class="modal-body">
				<%= bootstrap_form_for(item_concentration_unit, :class => "form-horizontal form-groups-bordered", :remote => true) do |f| %>
					<div class="errors"></div>
					<div class="col-md-12"> 
						<%= f.text_field :name %>
						<%= f.text_field :description %>
					</div>					
					<%= submit_button_tag(f.object) %>
				<% end %>
			</div>
		</div>
	</div>
</div>